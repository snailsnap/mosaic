image:
- Visual Studio 2017
configuration:
- Release
- Debug
before_build:
- cmd: |
    mkdir build
    cd build
    cmake --version
    cmake .. -G "Visual Studio 15 2017 Win64"
build:
  project: build\SnailSnap.sln
  verbosity: minimal
  parallel: true
