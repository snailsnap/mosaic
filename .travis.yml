sudo: false 
dist: trusty
language: cpp

os: linux
env: COMPILER_NAME=gcc CXX=g++-5 CC=gcc-5
addons:
  apt:
    packages:
      - g++-5
      - qtbase5-dev
    sources: &sources
      - llvm-toolchain-precise-3.8
      - ubuntu-toolchain-r-test

script:
  - cmake .
  - make
